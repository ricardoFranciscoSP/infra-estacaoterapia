#############################################
# ESTACAO TERAPIA — SOCKET — PRODUÇÃO (PRD)
# TEMPLATE/EXEMPLO - COPIE PARA /opt/secrets/estacao-socket.env
#############################################

# ======================
# Ambiente
# ======================
NODE_ENV=production

# ======================
# Redis
# ======================
# IMPORTANTE: Use 'redis' como hostname - é o nome do serviço no Docker Swarm
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=1
REDIS_URL=redis://:SUA_SENHA_REDIS_AQUI@redis:6379/1
REDIS_PASSWORD=SUA_SENHA_REDIS_AQUI

# ======================
# PostgreSQL
# ======================
# IMPORTANTE: Estas variáveis são OBRIGATÓRIAS para o socket-server funcionar
# O script entrypoint.sh constrói a DATABASE_URL a partir destas variáveis
# 
# ⚠️ ATENÇÃO: As credenciais abaixo DEVEM corresponder às credenciais configuradas
# no arquivo /opt/secrets/postgres.env usado pelo serviço PostgreSQL.
# Se você não tiver certeza das credenciais, verifique o arquivo postgres.env
# ou consulte o administrador do sistema.
# 
# Exemplo: Se o PostgreSQL está configurado com POSTGRES_USER=postgres e
# POSTGRES_DB=postgres, use essas mesmas credenciais aqui.
PG_HOST=pgbouncer
PG_PORT=6432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=SUA_SENHA_POSTGRES_AQUI
POSTGRES_DB=postgres

# ======================
# API Principal (Para comunicação interna)
# ======================
# URL da API principal para requisições internas do socket-server
# No Docker Swarm, os serviços se comunicam pelo nome do serviço na mesma rede Docker
# O serviço da API se chama "api" e roda na porta 3333 em produção
# Pode ser sobrescrita via API_BASE_URL ou API_URL
API_BASE_URL=http://api:3333

# ======================
# CORS (Produção)
# ======================
CORS_ORIGIN=https://estacaoterapia.com.br,https://www.estacaoterapia.com.br

